# 🎯 خطوات تفعيل النكهات والأحجام في قاعدة البيانات

## 1️⃣ تشغيل SQL في Supabase

انتقل إلى **Supabase Dashboard** → **SQL Editor** وقم بتشغيل الكود من ملف `add_product_variants.sql`:

```sql
ALTER TABLE products 
ADD COLUMN flavors TEXT DEFAULT NULL,
ADD COLUMN sizes_prices TEXT DEFAULT NULL;

COMMENT ON COLUMN products.flavors IS 'JSON array of product flavors, e.g. ["فانيليا", "شوكولاتة"]';
COMMENT ON COLUMN products.sizes_prices IS 'JSON array of sizes and prices, e.g. [{"size":"صغير","price":50}, {"size":"كبير","price":75}]';

CREATE INDEX idx_products_flavors ON products USING gin (to_tsvector('simple', flavors));
```

## 2️⃣ بيانات الحقول الجديدة

### flavors
- **نوع البيانات**: TEXT (JSON array)
- **الشكل**: `["فانيليا", "شوكولاتة", "فراولة"]`
- **مثال في قاعدة البيانات**:
```json
["فانيليا", "شوكولاتة", "فراولة"]
```

### sizes_prices
- **نوع البيانات**: TEXT (JSON array)
- **الشكل**: `[{"size":"صغير","price":50}, {"size":"كبير","price":75}]`
- **مثال في قاعدة البيانات**:
```json
[
  {"size":"صغير","price":50},
  {"size":"كبير","price":75},
  {"size":"عائلي","price":120}
]
```

## 3️⃣ الاستخدام في الإدارة

### إضافة منتج بنكهات وأحجام:
1. اذهب إلى **لوحة التحكم** → **المنتجات**
2. املأ بيانات المنتج
3. أضف **النكهات**: فانيليا، شوكولاتة، إلخ
4. أضف **الأحجام والأسعار**: صغير (50)، كبير (75)، إلخ
5. انقر **إضافة المنتج**

### تعديل المنتج:
1. اضغط على زر **تعديل** ✏️
2. عدّل النكهات والأحجام كما تشاء
3. انقر **حفظ التعديلات**

## 4️⃣ العرض في صفحة المنيو

- ✅ يظهر اسم المنتج
- ✅ يظهر الوصف
- ✅ **النكهات**: تظهر كـ "النكهات: فانيليا • شوكولاتة"
- ✅ **الأحجام والأسعار**: تظهر كـ "صغير: 50 ج.م | كبير: 75 ج.م"
- ✅ إذا كانت هناك أحجام، يتم إخفاء السعر الأساسي

## 5️⃣ الملفات المعدّلة

- ✅ `admin-script.js` - دوال إضافة/تحديث/قراءة البيانات
- ✅ `admin.html` - نماذج إضافة وتعديل مع حقول النكهات والأحجام
- ✅ `menu.html` - عرض النكهات والأحجام في الكارت
- ✅ `add_product_variants.sql` - SQL migration

## 🎉 الآن كل شيء يعمل من قاعدة البيانات!
